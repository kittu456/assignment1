#include <iostream>
using namespace std;

int main() {
	int a, b, c;
	cout << "Ennter two integers: ";
	cin >> a >> b;
	// sum of two numbers is stored in variable c
	c = a + b;
	
	// prints sum
	cout << a << "+" << b << "=" << c;
	
	return 0;
}
